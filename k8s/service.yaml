apiVersion: v1
kind: Service
metadata:
  name: up-poc-api
  namespace: up-poc
spec:
  selector:
    app: up-poc-api
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
  type: NodePort
